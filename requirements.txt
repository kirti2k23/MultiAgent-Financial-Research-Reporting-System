# All Python dependencies
# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt
# Autonomous Financial Research Agent
#
# Install with uv (fast):
#   uv pip install -r requirements.txt
#
# Install with pip (standard):
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────


# ── LLM & Agent Orchestration ─────────────────────────────────────────────────
# LangGraph — multi-agent state graph (the core of our pipeline)
langgraph>=0.2.0
# LangChain — base framework for LLM calls and chains
langchain>=0.3.0
# LangChain OpenAI — GPT-4o integration
langchain-openai>=0.2.0
# LangChain Community — extra tools and loaders (SEC filing support)
langchain-community>=0.3.0
# OpenAI — direct API client
openai>=1.40.0


# ── Data Validation ───────────────────────────────────────────────────────────
# Pydantic — AgentState, ResearchReport, all structured schemas
pydantic>=2.7.0


# ── Stock Data ────────────────────────────────────────────────────────────────
# yfinance — Yahoo Finance wrapper for live stock prices and metrics
yfinance>=0.2.40
# pandas — yfinance returns DataFrames; also used in data processing
pandas>=2.2.0
# numpy — numerical operations
numpy>=1.26.0,<2.0.0


# ── News & Web Search ─────────────────────────────────────────────────────────
# tavily-python — Tavily API client for financial news search
tavily-python>=0.3.0


# ── Sentiment Analysis ────────────────────────────────────────────────────────
# textblob — lightweight NLP for sentiment scoring on news articles
textblob>=0.18.0


# ── SEC Filing Retrieval ──────────────────────────────────────────────────────
# requests — HTTP calls to SEC EDGAR API
requests>=2.32.0
# beautifulsoup4 — HTML parsing for SEC filing pages
beautifulsoup4>=4.12.0


# ── RAG Pipeline ──────────────────────────────────────────────────────────────
# chromadb — local vector database to store SEC filing embeddings
chromadb==0.4.24
# tiktoken — token counting for chunking documents accurately
tiktoken>=0.7.0


# ── API Layer ─────────────────────────────────────────────────────────────────
# fastapi — REST API framework (exposes pipeline as /analyze endpoint)
fastapi>=0.112.0
# uvicorn — ASGI server to run FastAPI
uvicorn>=0.30.0

# ── Dashboard ─────────────────────────────────────────────────────────────────
# streamlit — web UI for the research agent dashboard
streamlit>=1.35.0


# ── Email Delivery ────────────────────────────────────────────────────────────
# sendgrid — sends the final research report by email
sendgrid>=6.11.0


# ── Observability ─────────────────────────────────────────────────────────────
# langsmith — traces every agent run in LangSmith dashboard
langsmith>=0.1.90


# ── Environment ──────────────────────────────────────────────────────────────
# python-dotenv — loads .env file into environment variables
python-dotenv>=1.0.0

# ── Evaluation ───────────────────────────────────────────────────────────────
# ragas — RAG pipeline evaluation framework
ragas==0.4.3
# datasets — required by ragas for evaluation datasets
datasets>=2.20.0

# ── Compatibility fixes ───────────────────────────────────────────────────────
# protobuf pinned for ChromaDB compatibility
protobuf==3.20.3